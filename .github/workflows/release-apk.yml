name: Release APK

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build & Release APK
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Setup Flutter 3.44.2
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.44.2"
          channel: stable
          cache: true

      - name: Show Flutter version
        run: flutter --version

      - name: Patch onnxruntime plugin (compileSdk 34)
        run: |
          PLUGIN_GRADLE="$HOME/.pub-cache/hosted/pub.dev/onnxruntime-1.4.1/android/build.gradle"
          if [ -f "$PLUGIN_GRADLE" ]; then
            sed -i 's/compileSdkVersion 33/compileSdkVersion 34/' "$PLUGIN_GRADLE"
            echo "Patched $PLUGIN_GRADLE"
          fi
        working-directory: app

      - name: flutter pub get
        run: |
          flutter pub get
          # Patch nach pub get (Cache wurde gerade angelegt)
          PLUGIN_GRADLE="$HOME/.pub-cache/hosted/pub.dev/onnxruntime-1.4.1/android/build.gradle"
          if [ -f "$PLUGIN_GRADLE" ]; then
            sed -i 's/compileSdkVersion 33/compileSdkVersion 34/' "$PLUGIN_GRADLE"
            echo "Patched $PLUGIN_GRADLE"
          fi
        working-directory: app

      - name: Setup release keystore
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          if [ -z "$ANDROID_KEYSTORE_BASE64" ]; then
            echo "::warning::ANDROID_KEYSTORE_BASE64 fehlt - APK wird mit Debug-Key signiert (OTA-Updates brechen!)"
            exit 0
          fi
          echo "$ANDROID_KEYSTORE_BASE64" | base64 -d > app/android/app/opa-release.jks
          cat > app/android/key.properties <<EOF
          storeFile=app/opa-release.jks
          storePassword=$ANDROID_KEYSTORE_PASSWORD
          keyAlias=$ANDROID_KEY_ALIAS
          keyPassword=$ANDROID_KEY_PASSWORD
          EOF
          echo "Release-Keystore eingerichtet: $(ls -la app/android/app/opa-release.jks)"

      - name: Extract version from tag
        id: version
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          VERSION="${TAG#v}"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Building $TAG (version=$VERSION)"

      - name: Build APK (release, split per ABI)
        run: |
          flutter build apk \
            --release \
            --split-per-abi \
            --build-name="${{ steps.version.outputs.version }}" \
            --build-number="${{ github.run_number }}" \
            --dart-define=GITHUB_RELEASE_TOKEN="${{ secrets.GH_RELEASE_READ_TOKEN }}" \
            --dart-define=BACKEND_BASE_URL="${{ vars.BACKEND_BASE_URL }}"
        working-directory: app

      - name: Rename APKs
        run: |
          cd app/build/app/outputs/flutter-apk
          ls -la
          for f in app-*-release.apk; do
            mv "$f" "opa-macht-auge-${{ steps.version.outputs.tag }}-${f#app-}"
          done
          ls -la

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: ${{ steps.version.outputs.tag }}
          generate_release_notes: true
          files: |
            app/build/app/outputs/flutter-apk/opa-macht-auge-*.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
